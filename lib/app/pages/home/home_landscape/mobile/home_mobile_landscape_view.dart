import 'package:flutter/widgets.dart';

class HomeMobileLandscapeView extends StatelessWidget {
  const HomeMobileLandscapeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('1');
  }
}
