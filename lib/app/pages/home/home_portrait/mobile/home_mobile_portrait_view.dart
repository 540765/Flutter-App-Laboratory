import 'package:flutter/widgets.dart';

class HomeMobilePortraitView extends StatelessWidget{
  const HomeMobilePortraitView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('211');
  } 
}